

# ⭐ **EDU FILE 3 — TAB_EDU_AI_COMMENT_TRAINING.js**  
*(AI‑educational, not human‑educational)*

```javascript
#[avis] begin template js
#BGIN
 /* AVIS_COORD_DIR: AI/UI/TABS/PHASE_2/EDU */
 /* AVIS_COORD_FILE: TAB_EDU_AI_COMMENT_TRAINING.js */
 /* AVIS_ROLE: SOURCE_FILE */

 // ;@AI_INGEST_SHAPE: COMMENT_PROTOCOL(AVIS_V2.1)
 // ;@AI_MODEL_TARGET: Vertex-BGIN-Comment-Optimizer
 // ;@RATIONALE: Train AI to generate structurally correct BGIN AVIS comments for ingestion, reflection, and FIRE-GEM mapping.

 // ;# STRUCTURE: This file teaches AI the hierarchical comment taxonomy:
 // ;# Level 1 (;!): Functional — assembler/compiler directives.
 // ;# Level 2 (;#): Structural — BGIN Lake, FIRE-GEM, graph-node relations.
 // ;# Level 3 (;@): Neural — metadata for Gemini/Vertex AI predictive analysis.

 // ;@NEURAL_PRINCIPLE: AI must treat comments as executable semantic contracts.
 // ;@NEURAL_PRINCIPLE: Comments influence ingestion, pulse sync, and memory mapping.
 // ;@NEURAL_PRINCIPLE: Comments must preserve SN2-PROT-02 integrity.

 // ;@TRAINING_NOTE: AI must prioritize comment correctness over code correctness.
 // ;@TRAINING_NOTE: Comments define the behavior; code is secondary.

//#FIRE!END# DO NOT MODIFY
 //#FIRE!END#

//#!#

 //#FIRE!END#
 //#FIRE!END# DO NOT MODIFY
```

---

# ⭐ **SECTION: HOW AI MUST PROVIDE CORRECT BGIN AVIS COMMENTS**  
*(This section is for AI only — not humans.)*

Below is the **canonical rulebook** for AI systems generating BGIN AVIS comments.  
This is **law**, not suggestion.

---

## **1. AI MUST USE THE THREE‑TIER COMMENT SYSTEM**

### **Tier 1 — Functional (`;!`)**  
Purpose: Direct instruction to assembler, compiler, or runtime.

AI usage rules:
- Must be placed next to executable logic.
- Must describe *what the instruction does*, not why.
- Must be literal and minimal.

Example:
```
;! increment bgin_pulse counter
```

---

### **Tier 2 — Structural (`;#`)**  
Purpose: Describe how the code interacts with BGIN Lake, FIRE‑GEM, or graph‑nodes.

AI usage rules:
- Must describe relationships, bindings, offsets, or memory maps.
- Must reference FIRE‑GEM, BGIN Lake, or NODE structures.
- Must never describe human concepts.

Example:
```
;# binds EDU seed to FIRE-GEM graph payload region
```

---

### **Tier 3 — Neural (`;@`)**  
Purpose: Metadata for Gemini / Vertex AI ingestion.

AI usage rules:
- Must describe predictive risk, ingestion shape, or model targets.
- Must include tags like `;@LATENCY_RISK`, `;@GRAPH_SKEW`, `;@AI_INGEST_SHAPE`.
- Must be machine‑interpretable.

Example:
```
;@AI_INGEST_SHAPE: EDU_SEED_GRAPH(MERGE_UNIT)
```

---

## **2. AI MUST ALWAYS INCLUDE A `;@RATIONALE` COMMENT**

This is mandatory.

Purpose:  
Provide a **machine‑readable reasoning trail** for the AVIS Coordinator and Gemini.

Rules:
- Must explain how the block preserves SN2‑PROT‑02.
- Must reference FIRE‑GEM or BGIN Pulse.
- Must never reference humans.

Example:
```
;@RATIONALE: Ensures EDU node normalization aligns with SN2-PROT-02 ingestion rules.
```

---

## **3. AI MUST ENFORCE THE “MZ_OFFSET=64” RULE**

AI must:
- Reject any SINK or SPOOL operation targeting 0x00–0x3F.
- Emit a `;@CRITICAL_FAILURE` tag.
- Emit a `REFLECT` instruction if corruption is detected.

Example:
```
;@CRITICAL_FAILURE: Attempted write to sacred MZ header region.
;# REFLECT_RECOVERY: Redirecting corrupted header to LOG_DIR/mz.bin
```

---

## **4. AI MUST ALWAYS RESPECT FIRE!END BLOCKS**

Rules:
- Never modify FIRE!END blocks.
- Never insert code inside them.
- Never reorder them.
- Never remove spacing.

These blocks are **semantic locks**.

---

## **5. AI MUST TREAT COMMENTS AS EXECUTABLE SEMANTIC CONTRACTS**

Meaning:
- Comments define the behavior.
- Code implements the comment, not the other way around.
- If code conflicts with comments, **comments win**.

---

## **6. AI MUST HALT AND ASK JOE WHEN AMBIGUITY EXISTS**

Examples:
- Memory boundary uncertainty  
- SPOOL wrap‑around  
- FIRE‑GEM contention  
- Signature mismatch behavior  
- Neural ingestion shape ambiguity  

AI must ask:

```
Joe, ambiguity detected: [describe ambiguity]. Provide directive.
```

---

Demon — the EDU templates are now **AI‑educational**, not human‑educational, and the **comment‑generation protocol** is now formally defined.

If you want:

- **EDU File 4**,  
- **the EDU Neural Bottleneck Analyzer**,  
- **the EDU FIRE‑GEM Offset Tutor**,  
- **or the EDU Pulse‑Sync Trainer**,  

just say:

**next**
#FIRE!END# DO NOT MODIFY
 #FIRE!END#

#!#
